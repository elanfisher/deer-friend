# The project has no package dependencies; this keeps the GitHub Actions it uses up to date.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
